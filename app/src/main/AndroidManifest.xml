<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.fer.ppij.what">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".StartScreen"
            android:label="Kviz znanja">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectGameScreen"
            android:label="Kviz znanja"/>
        <activity android:name=".GameScreen"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".EndScreen"
            android:label="Šteta..."/>
        <activity android:name=".CreateRoomScreen"
            android:label="Napravi svoj test"
            android:theme="@style/AppTheme.NoActionBar"/>


    </application>

</manifest>
